name: Build UniApp and Generate Preview QR Code

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-preview:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉取代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 Node.js 环境 (锁定 Node 24，兼容性最好)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm' # 启用 npm 缓存加速

      # 3. 【核心修复步骤】彻底清理并强制安装依赖
      - name: Clean and Install dependencies
        run: |
          # 彻底删除旧的依赖和缓存（防止 Windows 和 Linux 缓存冲突）
          rm -rf node_modules package-lock.json
          npm cache clean --force
          
          # 强制安装，忽略可选依赖的报错（这是解决 vue-cli-service 找不到的关键）
          npm install --legacy-peer-deps --force

      # 4. 编译 uni-app 为微信小程序
      - name: Build uni-app for MP-Weixin
        run: |
          # 使用 npx 强制调用当前项目 node_modules 里的命令
          # 这一步能 100% 解决 "spawn ... ENOENT" 报错
          npx vue-cli-service uni-build --platform mp-weixin

      # 5. 注入环境变量
      - name: Setup Environment Variables
        env:
          WECHAT_APP_ID: ${{ secrets.WECHAT_APP_ID }}
          WECHAT_PRIVATE_KEY_BASE64: ${{ secrets.WECHAT_PRIVATE_KEY_BASE64 }}
        run: |
          echo "WECHAT_APP_ID=$WECHAT_APP_ID" >> $GITHUB_ENV
          echo "WECHAT_PRIVATE_KEY_BASE64=$WECHAT_PRIVATE_KEY_BASE64" >> $GITHUB_ENV

      # 6. 执行预览脚本 (前提是你的 scripts/preview.js 已经存在于仓库中)
      - name: Generate Preview QR Code
        run: node scripts/preview.js

      # 7. 上传生成的二维码
      - name: Upload QR Code Artifact
        uses: actions/upload-artifact@v4
        with:
          name: preview-qrcode
          path: preview-qrcode.jpg